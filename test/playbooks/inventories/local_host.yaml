all:
  children:
    test_runner:
      hosts:
        localhost:
          ansible_connection: local
          ansible_user: "{{ lookup('ansible.builtin.env', 'USER') }}"
          ansible_host: localhost
